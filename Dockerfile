# not ready yet
# FROM python:3.10

# #RUN apt install git cargo

# #RUN git clone https://github.com/Nicbudd/16b64
# #WORKDIR /16b64/i16b64
# #RUN cargo build --release
# #ENV PATH="${PATH}:/16b64/i16b64/target/release"

# WORKDIR /app

# COPY . .
# # COPY ./.cdsapirc /root/ 
# RUN pip install -r requirements.txt

# CMD [ "python", "./bot.py" ]
